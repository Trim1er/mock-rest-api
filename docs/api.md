# API

## GET /users
Returns a static list of users.

## GET /items
Query params:
- `page` (default 1)
- `limit` (default 10)
- `sortBy` (`id|name|price`, default `id`)
- `order` (`asc|desc`, default `asc`)
